# Tempo 开发节点记录

这个文件记录项目的实际开发过程。每次完成一个明确节点后，更新“状态、变更、验证结果和下一步”，并与对应代码一起提交。

## 节点状态定义

- `done`：代码已完成，并通过对应验证。
- `partial`：有可运行实现，但仍有明确缺口。
- `blocked`：受到环境、设备或外部依赖阻塞。
- `planned`：已设计，尚未开始。

## 当前节点

### N-001 · 工程结构整理

- 状态：`done`
- 内容：将原始目录整理为 `android/`、`backend/`、`prototype/`、`docs/`。
- 结果：增加根 README、`.gitignore`、架构文档和 API 文档。
- 验证：Git 仓库已完成首个正式版本提交。

### N-002 · Android Compose 原型

- 状态：`partial`
- 内容：完成竖屏入口、底部导航、月/周/日/日程页面、日期切换、创建日程弹窗和 15 分钟选择器。
- 已知缺口：部分好友、通知和邀约数据仍为示例数据；尚未连接后端。
- 下一步：抽离 ViewModel 和 Repository，去除页面内硬编码数据。

### N-003 · 本地后端 MVP

- 状态：`partial`
- 内容：Python 标准库 HTTP 服务 + SQLite，支持健康检查、日程、邀约和基础时间扫描。
- 验证：`python3 -m py_compile backend/server.py` 通过；健康检查接口曾验证可返回成功结果。
- 已知缺口：没有账户认证、好友关系、完整多人匹配和事务锁。
- 下一步：拆分路由/服务/数据库层，增加接口测试。

### N-004 · Android 构建与设备验证

- 状态：`partial`
- 内容：解决 JVM target 不一致问题，项目曾在 Android Studio 中完成 Gradle 同步并构建。
- 当前阻塞：命令行重新构建需要下载 Gradle，受当前网络/权限环境影响，尚未在本次整理后再次独立验证。
- 下一步：在开发机 Android Studio 中执行 `Sync Project with Gradle Files` 和 `Build → Rebuild Project`。

### N-005 · 前后端联调

- 状态：`planned`
- 目标：Android 通过 Retrofit/OkHttp 调用本机或树莓派后端。
- 完成标准：创建日程后重启 App 数据仍存在；后端接口返回错误时页面能显示重试状态。

### N-006 · 账户与好友

- 状态：`planned`
- 目标：完成用户、登录、好友关系和服务端隐私过滤。
- 完成标准：好友只能获取状态色块和时间范围，不能获取标题、描述和分类。

### N-007 · 单人匹配与邀约状态机

- 状态：`planned`
- 目标：完成绿色扫描、黄色机动扫描、发起方锁定和接收方比价机制。
- 完成标准：完成发起、待应答、同意、拒绝、撤回、失效和冲突释放全链路。

### N-008 · 群组接龙

- 状态：`planned`
- 目标：完成群主、成员、最低人数、二次确认和失败重算。
- 完成标准：多人可以完成一次成功成团和一次失败重算。

### N-009 · 通知与兼容性

- 状态：`planned`
- 目标：完成 App 内通知、推送、Android 设备矩阵、树莓派部署和网络稳定性验证。
- 完成标准：目标设备能稳定连接后端，核心状态变化可及时同步。

### N-010 · Android 前端状态层重构

- 状态：`partial`
- 目标：让页面只负责展示，日程数据由 ViewModel 和 Repository 统一管理。
- 已完成：新增 `CalendarViewModel`、`MockCalendarDataSource`；创建日程通过 ViewModel 更新，月/周/日/日程视图共享同一份前端演示状态。
- 已完成：补充 `lifecycle-viewmodel-compose` 依赖。
- 已完成：已有日程可以点击进入编辑，修改名称、时间和状态；删除操作增加二次确认，并通过 ViewModel 从所有视图移除。
- 验证结果：Android Studio 从 `android/` 目录打开后，Gradle 编译成功，当前修改未发现编译错误。
- 已完成：好友、待应答邀约、通知和群组演示数据移出页面，统一由 `MockContentDataSource` 提供。
- 已调整：移除 Android 前端的 `SharedPreferences` 和本地持久化；前端只保留内存 Mock 数据，正式数据统一由后端 API 提供。
- 已完成：时间选择器打开时会回显已有起止时间。
- 已知缺口：页面组件仍集中在 `MainActivity.kt`；API 客户端尚未接入。
- 下一步：拆分页面文件，并实现纯 API 边界的 Remote Repository。
- 已完成：增加 `AuthRepository`、`SyncRepository` 和 Android 网络权限作为后端接入边界。
- 已完成：加入 Retrofit/OkHttp、加密 Token 本地保存、登录/注册页面和 Room 离线快照仓库；日程页面尚未替换 Mock 数据。
- 已完成：日程页面启动时读取同步快照，创建/编辑/删除日程调用真实后端 API；网络失败时读取 Room 快照。
- 已完成：未登录用户可以进入游客日历，基础日程写入 Room；登录用户继续使用后端同步。
- 已完成：移除周子昂、许知远、陈一一等预留好友和邀约数据；“我的”页面增加用户信息和深色/浅色/跟随系统三档主题选择。
- 已完成：接入 `logo.png`，用于 Android 应用图标和登录/注册页面。
- 已完成：Android 对外应用名称统一为 `Tempo`，暂不修改包名。
- 已完成：裁剪 Logo 外部多余白边；深色主题使用深蓝高亮，浅色主题使用天蓝高亮。
- 已完成：时间选择器扩展为 00:00–24:00 的 15 分钟网格，创建日程未选择时间时显示居中的白色提示按钮。
- 已完成：增加本地日程分类管理，支持预置分类和自定义标签及颜色；日视图显示完整时间区间。
- 已完成：在“我的”中增加分类标签统一管理和删除操作；时间按钮空状态跟随主题显示。
- 已完成：创建新日程的 15 分钟网格显示当天已有日程，已有时间段仅作提示，不阻止重复选择。

### N-011 · 后端账户与离线同步基础

- 状态：`partial`
- 目标：建立正式后端边界，账户数据由服务端管理，Android 不直接接触数据库。
- 已完成：新增 FastAPI + SQLAlchemy 后端结构，包含配置、数据库、模型、认证、日程、邀约和同步路由。
- 已完成：用户注册、用户名/邮箱登录、PBKDF2-SHA256 密码摘要、会话 Token 和当前用户接口。
- 已完成：增加用户、会话、日程、邀约、万年历缓存表，并提供同步快照接口。
- 已完成：补充好友关系、通知、群组和群成员表；增加数据库初始化脚本和数据库健康检查。
- 已完成：增加统一请求校验错误格式，便于 Android 和 Web 客户端处理。
- 已完成：创建硬性日程时执行服务端冲突校验；所有业务接口要求 Bearer Token。
- 验证结果：`python3 -m compileall -q backend/app` 通过；依赖安装和接口联调待在开发机执行。
- 用户已在本机实际验证健康检查、注册、Token 用户校验、日程创建/查询/修改/删除和同步快照，全部通过。
- 已知缺口：好友、群组、通知、刷新 Token、数据库迁移和 Android 邀约网络客户端尚未完成。
- 下一步：保持日历本地存储，接入真实好友和邀约 API。
- 下一步：将邀约和万年历页面进一步接入真实 API，并处理 Token 过期和增量同步。

### N-013 · 本地日历与账户网络边界

- 状态：`partial`
- 目标：日历基本功能脱离登录和网络，账号与邀约保持服务端交互边界。
- 已完成：日历事件统一写入 Room；游客可以查看、创建、编辑和删除本地日程。
- 已完成：登录用户也使用本地 Room 管理日历，登录状态只用于账户身份和后续邀约功能。
- 已完成：登录/注册页面增加“返回日历”按钮，并支持系统返回键。
- 已知缺口：邀约页面尚未连接真实好友和邀约 API；账号切换后的本地数据迁移策略待完善。
- 下一步：实现真实好友和邀约接口，日历数据继续保持本地存储。

### N-012 · 后端接口实测

- 状态：`done`
- 目标：确认后端基础链路在开发机上可以实际运行。
- 已完成：用户在本机验证健康检查、用户注册、Token 用户校验、日程创建、查询、修改、删除和同步快照。
- 验证结果：全部接口返回预期结果，SQLite 数据正常持久化。
- 结论：后端基础架构可以进入 Android 真机联调和自动化测试阶段。
- 下一步：补充邀约接口测试、错误场景测试、Token 越权测试、Room 离线测试和树莓派部署验证。

### N-014 · Android 前端内测准备

- 状态：`done`
- 目标：确认当前 Android 前端已经具备交给朋友进行真机体验和反馈收集的条件。
- 已完成：日历月/周/日/日程视图、日期切换、创建/编辑/删除日程、15 分钟时间选择、标签管理、主题切换、登录/注册页面和游客离线日历均可使用。
- 已完成：修复登录页主题不生效、`Surface` 缺少导入、标签全部删除后的边界崩溃等问题。
- 已完成：README 增加 Debug APK 生成、ADB 安装、局域网后端访问说明和内测反馈模板。
- 验证结果：最近一次 Kotlin 编译错误已修复，`git diff --check` 通过；正式发放前仍需在 Android Studio 执行一次 `Build → Rebuild Project` 并生成 APK。
- 当前限制：真实好友、多人邀约、推送和正式联网同步尚未完成；当前内测重点是本地日历体验、主题、标签和页面交互。
- 下一步：收集至少 2–3 名测试者的真机反馈，按“阻塞问题 → 功能问题 → 视觉问题 → 兼容性问题”排序修复。

### N-015 · 当前日期与视图滑动优化

- 状态：`done`
- 目标：修复日历日期写死和周视图起始日错误，提升手机端视图切换体验。
- 已完成：月、周、日视图初始化为 Android 设备系统当前日期，不再固定为 2026 年 7 月 14 日。
- 已完成：周视图按周一作为第一天计算，并支持跨月的一整周显示。
- 已完成：月、周、日视图支持从左向右或从右向左滑动切换。
- 已完成：创建日程入口默认使用当前系统日期。
- 验证结果：`git diff --check` 通过；需要在 Android Studio 中重新构建并在真机上验证滑动手势和日期边界。
- 下一步：根据内测反馈继续优化手势冲突、日期数据准确性和不同屏幕尺寸适配。

### N-016 · 内测反馈：滑动与月视图修复

- 状态：`done`
- 已完成：将视图切换手势从拖动过程实时切换改为手指抬起后判断一次，减少页面重复重组和滑动卡顿。
- 已完成：移除月视图底部重复渲染的当天日程卡片，避免其遮挡月历并造成错误显示。
- 验证结果：`git diff --check` 通过；需要在真机上继续观察不同屏幕尺寸下的滑动流畅度。
- 下一步：继续收集内测反馈，重点关注手势与日期点击、上下滚动之间的冲突。

### N-017 · 全页面手势与周视图滚动

- 状态：`done`
- 已完成：将月/周/日切换手势从内容卡片扩展到整个日历主体，包括标题、月份导航和视图选择区域。
- 已完成：增加左右滑动的进入/退出过渡动画。
- 已完成：月、周、日视图主体支持上下滚动；日程较多的周视图不再被底部导航遮挡。
- 验证结果：`git diff --check` 通过；需在 Android Studio 中重新构建并在真机上确认动画、点击和滚动手势没有冲突。
- 下一步：根据不同屏幕尺寸继续调整动画距离、滚动区域和触摸阈值。

### N-018 · 周视图空状态遮挡修复

- 状态：`done`
- 已完成：周视图仅在本周存在日程时渲染“本周日程”卡片。
- 已完成：移除无日程时的“本周还没有日程”占位文本，避免空状态卡片遮挡日期区域。
- 验证结果：`git diff --check` 通过；需要在真机上分别验证有日程和无日程两种状态。

### N-019 · 周视图 24 小时课程表

- 状态：`done`
- 已完成：在周日期栏下增加周课程表式时间网格，范围覆盖 `00:00–24:00`。
- 已完成：按日程开始时间和结束时间计算色块的纵向位置和高度，并显示标题及时间区间。
- 已完成：日程颜色沿用硬性、完全空闲、机动和待应答状态色；点击色块可以进入编辑。
- 已完成：时间网格支持横向查看七天，表头与网格共用滚动位置；外层页面支持上下滚动。
- 验证结果：`git diff --check` 通过；需要在 Android Studio 真机验证跨天、重叠和 00:00/24:00 边界日程。

### N-020 · 课程表编译错误修复

- 状态：`done`
- 问题：周课程表使用 `Float * Dp` 计算色块高度和位置，Kotlin 无法匹配对应运算符，导致 `compileDebugKotlin` 失败。
- 修复：改为 `Dp * Float`，明确使用 Compose 支持的单位计算方式。
- 验证结果：`git diff --check` 通过；需在 Android Studio 重新执行 Rebuild Project。

### N-021 · 周课程表单页布局

- 状态：`done`
- 已完成：周视图取消上下滚动，24 小时网格压缩为适合手机屏幕的单页布局。
- 已完成：课程表物块仅显示活动名称，减少小尺寸时间格内的信息拥挤。
- 已完成：物块颜色改为用户选择的分类标签颜色，保持和日程列表中的标签色一致。
- 验证结果：`git diff --check` 通过；需在不同屏幕尺寸上验证文字截断和横向七日布局。

### N-022 · 周课程表七列自适应

- 状态：`done`
- 已完成：移除左侧时间标注和横向滚动，周日到周六七列等宽铺满当前屏幕。
- 已完成：活动名称改为居中显示，长标题允许自动换行并限制最大行数。
- 验证结果：`git diff --check` 通过；需要在窄屏 Android 设备上确认七列宽度和文字可读性。

### N-023 · 周课程表 24 小时纵向校准

- 状态：`done`
- 已完成：将周课程表固定为 24 个等高小时区间，从 00:00 覆盖到 24:00。
- 已完成：日程色块仍按开始分钟和持续分钟计算纵向位置与高度。
- 验证结果：`git diff --check` 通过；需要在真机确认 00:00、12:00 和 23:00 附近日程的定位。

### N-024 · 课程表时长比例修复

- 状态：`done`
- 问题：不同起止时间的日程色块在部分布局约束下显示为相同高度。
- 修复：使用分钟数和每小时固定高度计算整数像素，并通过 `requiredHeight` 保证色块高度不被父布局压缩。
- 预期：16 小时日程高度为 24 小时日程的约 `2/3`，起始位置也按开始分钟偏移。
- 验证结果：`git diff --check` 通过；需要在真机创建 00:00–16:00 与 00:00–24:00 日程确认视觉比例。

### N-025 · 课程表边界裁剪

- 状态：`done`
- 已完成：将课程表日程起止时间限制在 `00:00–24:00` 范围内。
- 已完成：为每日网格增加边界裁剪，日程色块不会再超出最后一格。
- 验证结果：`git diff --check` 通过；需要在真机测试全天日程和接近 24:00 的日程。

### N-026 · 固定网格坐标布局

- 状态：`done`
- 已完成：课程表先固定 24 小时网格，再使用 `开始分钟 / 1440` 和 `持续分钟 / 1440` 计算色块坐标。
- 已完成：移除强制高度对父布局的影响，色块不会再撑开或脱离对应格线。
- 验证结果：`git diff --check` 通过；需要在真机验证 00:00–16:00、00:00–24:00 和中途开始的日程。

### N-027 · 周课程表剩余空间自适应

- 状态：`done`
- 已完成：周页面将课程表区域放入剩余空间，动态计算每小时高度，而不是使用固定总高度。
- 已完成：24 个小时格完整位于当前周页面内，底部不会再被页面边界裁剪。
- 已完成：日程色块继续使用同一网格高度计算位置和持续时间，全天日程不会超出网格。
- 验证结果：`git diff --check` 通过；需在 Android Studio 真机验证不同屏幕高度下的 24 格显示。

### N-028 · 周范围与重复表头修复

- 状态：`done`
- 已完成：周视图标题根据选中日期所在周动态显示；只有当前系统周显示“本周”，其他周显示具体日期范围。
- 已完成：删除课程表内部重复的日期表头，复用上方万年历周日期栏。
- 已完成：移除重复表头后，课程表剩余高度全部用于 24 小时网格。
- 验证结果：`git diff --check` 通过；需要真机验证当前周、下一周和跨月周的标题与日期一致性。

### N-029 · Tempo 共享服务器后端部署

- 状态：`partial`
- 已完成：在服务器创建 `tempo` 系统用户和 `/srv/tempo` 独立工作目录，后端不使用 root 运行。
- 已完成：上传后端代码，创建 `/srv/tempo/venv`，安装 FastAPI、Uvicorn、SQLAlchemy 等依赖。
- 已完成：增加 `deploy/tempo-backend.service`，使用 systemd 管理服务并设置开机自启。
- 已完成：初始化服务器独立 SQLite 数据库，数据库位于服务器 `/srv/tempo/app/calendar.db`。
- 已完成：服务器本机健康检查通过：`/api/health` 返回 `ok=true`，服务曾监听 `0.0.0.0:8765`。
- 已修复：兼容服务器 Python 3.9，将后端联合类型改为 `Optional` 写法。
- 已调整：经服务器所有者授权，停止原 PM2 `zongheng` 服务，Tempo 接管公网 TCP 3001。
- 下一步：验证 `1.95.175.42:3001` 手机访问，再配置 HTTPS 和数据库备份。

### N-030 · Android 真实认证联调准备

- 状态：`partial`
- 已完成：Android Retrofit API 地址切换到服务器 `http://1.95.175.42:3001/`。
- 已完成：服务器本机健康检查通过，认证路由无效登录测试返回预期的 `401`。
- 已完成：修复 Android 启动时服务器暂时不可达会错误清除本地 Token 的问题，保留本地缓存登录状态。
- 已调整：Android Release API 地址切换为 `http://1.95.175.42:3001/`。
- 下一步：使用 Android 真机完成注册、登录、退出和重新启动 App 恢复登录测试。

### N-031 · 隐私与安全边界确认

- 状态：`partial`
- 已确认：密码使用随机盐 PBKDF2-SHA256 摘要，服务端不保存明文密码。
- 已确认：登录 Token 为随机值，服务器只保存 Token 哈希；Android 使用 EncryptedSharedPreferences 和 Android Keystore 保存本地会话。
- 已确认：Android 日程和标签只写入本地 Room，当前 Android 不调用服务器日程接口。
- 已确认：服务器运行目录由 `tempo` 用户管理，`.env` 权限为受限权限，systemd 服务不以 root 运行。
- 当前限制：公网仍是 HTTP，密码可能被网络窃听；HTTPS 配置完成前只允许使用专用内测账号。
- 当前限制：Room 数据库尚未做数据库级加密，设备丢失防护待后续增加。
- 下一步：配置域名/HTTPS，增加登录失败限流和数据库备份，再进行正式账号联调。

### N-033 · 账号 ID 与昵称管理

- 状态：`done`
- 已完成：新增独立的对外 `account_id`，与数据库内部主键分离，避免账号规则影响外键关系。
- 已完成：账号 ID 从 `100001` 开始分配；服务器现有 `wcy` 账号已迁移为 `100001`。
- 已完成：新增 `PATCH /api/auth/me` 修改昵称接口，限制昵称长度并拒绝空昵称。
- 已完成：Android “我的”页面显示六位账号 ID，并增加“修改昵称”入口。
- 验证结果：服务器数据库迁移成功，当前用户记录为 `wcy / 100001`；健康检查通过。
- 下一步：用 Android 真机完成昵称修改、退出后重新登录和账号 ID 显示验证。

### N-032 · SSH 隧道内测链路

- 状态：`done`
- 已调整：Debug 和 Release 构建统一直接访问公网服务器 `http://1.95.175.42:3001/`。
- 已完成：补充 `ssh -N -L 8765:127.0.0.1:3001` 和 `adb reverse` 测试链路，不依赖其他端口配置。
- 使用方式：SSH 隧道仅作为公网端口不可用时的备用方案；当前公网 3001 已验证可用，手机无需隧道即可访问。
- 当前限制：公网仍使用 HTTP，正式使用前需要切换 HTTPS。
- 已验证：公网 `http://1.95.175.42:3001/api/health` 返回正常，Tempo 已接管 3001。

### N-034 · 好友、个人资料与退出误触保护

- 状态：`partial`
- 已完成：好友能力收拢到“找时间”页面，不新增底部导航；支持按用户名、昵称或六位账号 ID搜索并发送好友申请。
- 已完成：后端新增好友搜索、好友申请和好友关系列表接口，搜索结果只返回必要公开信息，不返回邮箱、电话、爱好和签名。
- 已完成：用户表预留电话、爱好、签名字段，并通过资料接口编辑；Android“我的”页面显示并编辑这些资料。
- 已完成：退出登录采用连续两次确认；系统返回退出应用改为 2 秒内连续按两次返回键直接退出，并用 Toast 提示第一次按键。
- 已保持：通知页面本轮不改，后续再评估是否改为弹窗。
- 验证结果：后端 Python 语法检查和 `git diff --check` 通过；Android 编译待在已配置 JDK 17 的 Android Studio 中验证。
- 下一步：真机联调好友搜索、好友申请、资料保存；再补充好友申请同意/拒绝和真实好友列表。

### N-035 · 好友搜索修复与取消搜索

- 状态：`done`
- 已修复：账号 ID 模糊查询错误导致的 HTTP 500；数字查询现在使用安全的整数精确匹配，用户名和昵称仍支持模糊匹配。
- 已完成：搜索结果增加“取消”操作，清空关键词和结果，避免结果长期停留造成误添加。
- 验证结果：服务器健康检查通过，Android `compileDebugKotlin` 编译成功。

### N-036 · 搜索范围与用户提示优化

- 状态：`done`
- 已调整：取消六位账号 ID 搜索和加好友入口，只允许使用用户名或昵称搜索。
- 已完成：搜索无结果显示明确提示，并提供“取消”按钮清空关键词和结果。
- 已完成：登录、注册、资料保存和好友申请将常见 HTTP 状态转换为用户可理解的中文提示，不再直接显示 HTTP 错误文本。
- 验证结果：服务器健康检查通过，Android `compileDebugKotlin` 编译成功。

### N-037 · 多账号、好友群组与异地登录规划

- 状态：`partial`
- 背景：当前好友申请和群组仍是早期原型；Room 本地数据尚未按账号隔离；服务器会话暂未提供设备级管理。
- 已完成基础后端：好友申请支持同意/拒绝；群组支持创建、查询和邀请已同意好友；群主权限在服务端校验。
- 已完成：Room 日程、邀请缓存和万年历缓存增加账号作用域；标签和主题改为按账号保存；登录状态变化后刷新本地日历。
- 已完成基础后端：每次登录创建独立会话，支持会话列表、当前会话注销和指定会话注销。
- 待完成：Android 好友申请处理、群组管理和登录设备管理页面；密码修改后注销全部旧会话。
- 存储原则：个人日程默认保存在本地；服务器保存账号、好友、群组、邀约和已确认活动等协作数据，不把所有私人日程无条件上传服务器。
- 实施顺序：本地多账号隔离 → 好友关系重构 → 群组关系重构 → 异地登录会话管理 → 可选日程同步。
- 验证结果：Android `compileDebugKotlin` 编译成功；服务器数据库迁移和 `/api/health` 检查通过。
- 真机验证：两个账号与游客模式的日程、标签、主题互不干扰；退出并重新登录后数据归属正确；数据库迁移未造成已有日程丢失。

### N-038 · 好友与会话基础接入

- 状态：`partial`
- 已完成：Android“找时间”读取真实好友关系，支持好友申请同意/拒绝、删除好友，并只将已同意好友用于邀约入口。
- 已完成：后端增加群组创建、查询和邀请已同意好友的基础接口；服务端校验群主权限。
- 已完成：后端增加多设备会话列表、当前会话注销和指定会话注销；Android退出登录时会尝试注销当前服务器会话，离线时仍可本地退出。
- 已验证：Android `compileDebugKotlin` 成功，后端 Python 语法检查通过，服务器数据库迁移和健康检查通过。
- 待完成：群组 Android 页面、登录设备管理页面、修改密码后注销全部旧会话。

### N-039 · 找时间页面与好友状态修复

- 状态：`partial`
- 已修复：客户端和服务端均阻止自己添加自己；好友申请和重复申请会显示中文提示。
- 已修复：找时间页面改为整体纵向滚动，不再使用嵌套列表遮挡好友区域。
- 已完成：个人日程保存时仅同步日期、开始/结束时间和红绿黄状态色块，不同步标题、分类和描述；好友只能查看已同意好友的状态色块。
- 已完成：好友列表增加“看时间”和“删除”入口。
- 验证结果：Android `compileDebugKotlin` 成功，好友状态接口已部署并完成服务器健康检查。
- 待验证：两台设备分别创建日程后，好友页面能否显示对应日期的状态色块。

### N-040 · 找时间交互调整

- 状态：`partial`
- 已完成：“看时间”改为弹窗展示好友当天的红/绿/黄状态色块。
- 已完成：添加好友与现有好友分区显示；找时间页面整体支持纵向滚动。
- 已完成：发起邀约取消固定活动时长，改为开始/结束时间的 15 分钟粒度选择，并展示可选择的完整时间段。
- 已完成：将邀约扫描原型替换为后端真实候选算法：先计算双方绿色完全空闲交集；无满足时长时，再将黄色机动和未标记时间作为可匹配时间计算。

### N-041 · 真实邀约链路与候选生成

- 状态：`partial`
- 已完成：新增 `POST /api/invites/match`，按 15 分钟网格生成连续完整时间段，返回开始时间、结束时间、匹配类型和评分。
- 已完成：Android 发起邀约先选择好友、活动时长，再扫描候选；候选显示完整时间范围，选择后发送真实邀约。
- 已完成：发起方待应答卡片显示“取消邀约”；接收方显示“拒绝/同意”。
- 已完成：接收方同意后，Android 将已确认邀约写入本地日历，状态为硬性邀约；服务端自动拒绝同一接收方同时间重叠的其他待应答邀约。
- 已完成：接收方“看时间”使用周视图课程表形式查看好友状态色块。
- 验证结果：后端编译、数据库初始化、服务器健康检查通过；Android `:app:compileDebugKotlin` 编译成功。
- 已知边界：当前算法依赖客户端发布到服务端的红/绿/黄状态块；发起方灰色锁定、接收方改红自动失效、群组撮合和完整通知推送仍需继续实现。
- 下一步：补充邀约取消后的本地状态刷新、接收方改红联动、群组多人候选和更细的离线同步策略。

### N-042 · 邀约流程改为“时长驱动”

- 状态：`done`
- 原问题：让发起人先选择一个开始/结束时间，再扫描双方空闲，会把系统匹配结果限制在一个没有实际意义的固定区间内。
- 已调整：发起邀约时只选择活动持续时间，不再选择开始时间和结束时间。
- 已调整：后端从当前日期起扫描未来 7 天的 15 分钟网格，自动识别双方多个连续空闲时间段。
- 已调整：先返回双方完全空闲的候选；没有候选时，再返回包含机动或未标记时间的候选。
- 已调整：用户最后只需要从系统生成的完整候选时间段中选择一个，再发送邀约。
- 已调整：活动时长改为滚轮式选择器，界面只显示实际时长，当前支持 15 分钟至 8 小时。
- 验证结果：Android `:app:compileDebugKotlin` 编译成功。

### N-043 · 邀约匹配范围粗选

- 状态：`partial`
- 已完成：新增未来 7 天周表格范围选择器，横向为日期，纵向为 00:00–24:00。
- 已完成：用户通过左上和右下两个点粗略框选允许匹配的日期与时间范围，例如晚饭可框选 17:00 之后的多个日期。
- 已完成：匹配接口接收绝对开始/结束时间，只在框选范围内生成候选时段；不框选时仍扫描未来 7 天全天。
- 已调整：范围语义改为真正的矩形区域，即日期区间 × 每天的时间区间；不再将两个点解释为一条跨天连续时间线。
- 已调整：活动时长滚轮最高支持 8 小时，界面只显示实际时长，不显示内部 15 分钟格数量。
- 验证结果：Android `:app:compileDebugKotlin` 编译成功，后端 Python 语法检查通过。
- 待验证：真机上检查跨天框选、边界 00:00/24:00 和无候选时的提示。

### N-044 · 双人邀约内测版本

- 状态：`done`
- 版本定位：当前版本可作为双人邀约功能内测版本发布。
- 已完成：好友关系接入、好友状态色块查看、活动时长选择、未来 7 天矩形可约范围框选。
- 已完成：后端按双方空闲状态生成候选时间段，绿色完全空闲优先，无结果时纳入黄色机动和未标记时间。
- 已完成：候选以完整开始/结束时间显示，用户选择后发送真实邀约。
- 已完成：发起方可取消，接收方可同意或拒绝；同意后写入本地日历，重叠待应答邀约自动拒绝。
- 已完成：服务器后端部署、数据库连接、健康检查和 Android 编译验证。
- 验证命令：`python3 -m compileall -q backend/app`、`./gradlew :app:compileDebugKotlin --no-daemon`。
- 已知边界：群组撮合、推送通知、发起方灰色锁定、接收方改红自动失效以及正式 HTTPS 仍未纳入本次版本。
- 下一步：收集双人邀约内测反馈，再处理异常边界和群组多人撮合。

### N-045 · 双人邀约体验修复

- 状态：`done`
- 已修复：邀约候选列表改为独立可滚动区域，不再被弹窗高度截断。
- 已完成：发起方发送邀约后，本地立即写入蓝色“待应约”时间块，体现该时间已被暂时占用。
- 已完成：发起方取消邀约后删除本地锁定块。
- 已完成：接收方同意邀约后写入硬性日程，标题包含参与者，例如 `晚餐(with B)`。
- 已调整：待应约时间发布给服务端时按硬性占用处理，避免其他匹配再次使用发起方已锁定的时间。
- 验证结果：Android `:app:compileDebugKotlin` 编译成功。

### N-046 · 日历页面切换性能优化

- 状态：`done`
- 已优化：移除月/周/日切换时同时渲染进出场页面的大型横向动画，改为单页面即时切换，避免日历网格和课程表重复组合。
- 已优化：横向滑动手势不再随着视图模式反复重建，减少切换过程中的额外开销。
- 影响范围：日程与找时间页面切换、月/周/日/日程视图切换。
- 验证结果：Android `:app:compileDebugKotlin` 编译成功。
- 待真机验证：低端 Android 设备上的首帧耗时和连续快速切换体验。

### N-047 · 万年历数据与状态反馈

- 状态：`done`
- 已完成：新增独立 `CalendarInfoProvider` 数据层，替换页面内写死的农历计算逻辑。
- 已完成：内置 2026 年农历月首、农历日期、主要节气和主要节日数据；页面可显示节日和节气信息。
- 已完成：保留本地数据层扩展入口，后续可将 `CalendarDayInfo` 替换为 Room/服务端缓存数据。
- 已完成：日历、好友和邀约增加加载状态；日历网络失败时保留本地日程并提供重试/关闭提示；已有空数据提示继续保留。
- 已完成：更新 TODO，标记真实双人邀约 API、绿色优先匹配和机动兜底等已完成事项。
- 验证结果：Android `:app:compileDebugKotlin` 编译成功。
- 已知边界：当前本地万年历内置范围以 2026 年为主，其他年份显示“农历待同步”，后续接入完整 1900–2100 数据或服务端缓存。

### N-048 · 万年历最终数据方案

- 状态：`done`
- 已移除：页面内 2026 年农历、节气和节日硬编码。
- 已完成：接入标准农历库，离线计算农历、节气和传统节日。
- 已完成：新增 `CalendarDataRepository`，在用户授权后读取 Android 系统日历中的非全天事件，并与 Tempo 本地日程合并展示。
- 已完成：万年历计算结果写入 Room `cached_calendar_days`，为后续网络同步和离线缓存保留统一入口。
- 已完成：首次运行申请系统日历读取权限；拒绝权限时不影响 Tempo 本地日程使用。
- 验证结果：Android `:app:compileDebugKotlin` 编译成功。
- 已知边界：法定节假日调休安排仍需通过后端数据源同步，系统日历全天事件暂不写入 15 分钟时间网格。

## 每次开发节点的记录模板

```markdown
### N-XXX · 节点名称

- 状态：`planned`
- 目标：
- 变更文件：
- 已完成：
- 验证命令：
- 验证结果：
- 已知问题：
- 下一步：
```

## 变更日志

### 2026-07-16

- 建立完整阶段计划。
- 新增项目开发节点记录。
- 明确当前 Android 原型、后端 MVP 和联网联调的边界。
- 开始 Android 前端状态层重构，新增 ViewModel 和本地 Repository。
- 建立后端账户、密码安全和离线同步的第一版基础结构。
- 完成 Android 前端内测准备，补充 APK 分发、安装和反馈收集说明。
