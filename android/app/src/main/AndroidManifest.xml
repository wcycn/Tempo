<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 仅用于本机开发后端 http://127.0.0.1:8765；正式环境切换 HTTPS 后删除此属性。 -->
    <application android:usesCleartextTraffic="true" android:icon="@drawable/logo" android:roundIcon="@drawable/logo" android:theme="@style/Theme.HutongCalendar" android:label="Tempo" android:allowBackup="true" android:supportsRtl="true">
        <activity android:name=".MainActivity" android:screenOrientation="portrait" android:exported="true">
            <intent-filter><action android:name="android.intent.action.MAIN"/><category android:name="android.intent.category.LAUNCHER"/></intent-filter>
        </activity>
    </application>
</manifest>
